import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.neoavance.photoanalysis',
  appName: 'photosAnalysis',
  webDir: 'dist/photos-analysis',
  bundledWebRuntime: false
};

export default config;
